# Database connection
# For docker-compose (app in container): use "db" as host
# For devcontainer (app in dev container): use "host.docker.internal" or "db" if using docker-compose
# For local development (app on host): use "localhost"
DATABASE_URL="postgresql://crewuser:crewpass@localhost:5432/crewmgmt"

# NextAuth configuration
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
